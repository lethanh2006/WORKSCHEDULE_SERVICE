name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Shared Node.js quality gate
    uses: lethanh2006/Logger/.github/workflows/reusable-node-ci.yml@3edef57e4ab192832b7a2d5465b2948592a55125
    with:
      platform-ref: 3edef57e4ab192832b7a2d5465b2948592a55125
