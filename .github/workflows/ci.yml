name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Shared Node.js quality gate
    uses: lethanh2006/Logger/.github/workflows/reusable-node-ci.yml@8f26f109b44c5fb0454a911d9dd8c601350c6281
    with:
      platform-ref: 8f26f109b44c5fb0454a911d9dd8c601350c6281
